apiVersion: v1
kind: Service
metadata:
  name: wealth-management
  namespace: tpt-banking
  labels:
    app: wealth-management
spec:
  selector:
    app: wealth-management
  ports:
    - name: http
      port: 3009
      targetPort: 3009
    - name: metrics
      port: 9473
      targetPort: 9473
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: wealth-management
  namespace: tpt-banking
  labels:
    app: wealth-management
    app.kubernetes.io/component: api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: wealth-management
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: wealth-management
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9473"
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app: wealth-management
                topologyKey: kubernetes.io/hostname
      containers:
        - name: wealth-management
          image: tpt-banking/wealth-management:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3009
            - containerPort: 9473
          envFrom:
            - configMapRef:
                name: tpt-banking-config
            - secretRef:
                name: tpt-banking-secrets
          env:
            - name: PORT
              value: "3009"
            - name: PROMETHEUS_PORT
              value: "9473"
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          readinessProbe:
            httpGet:
              path: /v1/health
              port: 3009
            initialDelaySeconds: 15
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /v1/health
              port: 3009
            initialDelaySeconds: 30
            periodSeconds: 30
            failureThreshold: 3
          lifecycle:
            preStop:
              exec:
                command: ["/bin/sh", "-c", "sleep 10"]
      terminationGracePeriodSeconds: 60
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: wealth-management
  namespace: tpt-banking
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: wealth-management
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
