apiVersion: v1
kind: Service
metadata:
  name: open-banking
  namespace: tpt-banking
  labels:
    app: open-banking
spec:
  selector:
    app: open-banking
  ports:
    - name: http
      port: 3003
      targetPort: 3003
    - name: metrics
      port: 9467
      targetPort: 9467
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: open-banking
  namespace: tpt-banking
  labels:
    app: open-banking
    app.kubernetes.io/component: api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: open-banking
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: open-banking
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9467"
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app: open-banking
                topologyKey: kubernetes.io/hostname
      containers:
        - name: open-banking
          image: tpt-banking/open-banking:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3003
            - containerPort: 9467
          envFrom:
            - configMapRef:
                name: tpt-banking-config
            - secretRef:
                name: tpt-banking-secrets
          env:
            - name: PORT
              value: "3003"
            - name: PROMETHEUS_PORT
              value: "9467"
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          readinessProbe:
            httpGet:
              path: /v1/health
              port: 3003
            initialDelaySeconds: 15
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /v1/health
              port: 3003
            initialDelaySeconds: 30
            periodSeconds: 30
            failureThreshold: 3
          lifecycle:
            preStop:
              exec:
                command: ["/bin/sh", "-c", "sleep 10"]
      terminationGracePeriodSeconds: 60
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: open-banking
  namespace: tpt-banking
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: open-banking
  minReplicas: 2
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
