apiVersion: v1
kind: Service
metadata:
  name: regulatory-reporting
  namespace: tpt-banking
  labels:
    app: regulatory-reporting
spec:
  selector:
    app: regulatory-reporting
  ports:
    - name: http
      port: 3007
      targetPort: 3007
    - name: metrics
      port: 9471
      targetPort: 9471
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: regulatory-reporting
  namespace: tpt-banking
  labels:
    app: regulatory-reporting
    app.kubernetes.io/component: api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: regulatory-reporting
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: regulatory-reporting
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9471"
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app: regulatory-reporting
                topologyKey: kubernetes.io/hostname
      containers:
        - name: regulatory-reporting
          image: tpt-banking/regulatory-reporting:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3007
            - containerPort: 9471
          envFrom:
            - configMapRef:
                name: tpt-banking-config
            - secretRef:
                name: tpt-banking-secrets
          env:
            - name: PORT
              value: "3007"
            - name: PROMETHEUS_PORT
              value: "9471"
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          readinessProbe:
            httpGet:
              path: /v1/health
              port: 3007
            initialDelaySeconds: 15
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /v1/health
              port: 3007
            initialDelaySeconds: 30
            periodSeconds: 30
            failureThreshold: 3
          lifecycle:
            preStop:
              exec:
                command: ["/bin/sh", "-c", "sleep 10"]
      terminationGracePeriodSeconds: 60
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: regulatory-reporting
  namespace: tpt-banking
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: regulatory-reporting
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
