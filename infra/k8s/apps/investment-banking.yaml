apiVersion: v1
kind: Service
metadata:
  name: investment-banking
  namespace: tpt-banking
  labels:
    app: investment-banking
spec:
  selector:
    app: investment-banking
  ports:
    - name: http
      port: 3004
      targetPort: 3004
    - name: metrics
      port: 9468
      targetPort: 9468
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: investment-banking
  namespace: tpt-banking
  labels:
    app: investment-banking
    app.kubernetes.io/component: api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: investment-banking
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: investment-banking
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9468"
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app: investment-banking
                topologyKey: kubernetes.io/hostname
      containers:
        - name: investment-banking
          image: tpt-banking/investment-banking:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3004
            - containerPort: 9468
          envFrom:
            - configMapRef:
                name: tpt-banking-config
            - secretRef:
                name: tpt-banking-secrets
          env:
            - name: PORT
              value: "3004"
            - name: PROMETHEUS_PORT
              value: "9468"
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          readinessProbe:
            httpGet:
              path: /v1/health
              port: 3004
            initialDelaySeconds: 15
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /v1/health
              port: 3004
            initialDelaySeconds: 30
            periodSeconds: 30
            failureThreshold: 3
          lifecycle:
            preStop:
              exec:
                command: ["/bin/sh", "-c", "sleep 10"]
      terminationGracePeriodSeconds: 60
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: investment-banking
  namespace: tpt-banking
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: investment-banking
  minReplicas: 2
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
