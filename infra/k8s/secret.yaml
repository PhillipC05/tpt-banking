# TEMPLATE — do NOT commit real values. Populate via sealed-secrets, external-secrets, or Vault agent injection.
apiVersion: v1
kind: Secret
metadata:
  name: tpt-banking-secrets
  namespace: tpt-banking
type: Opaque
stringData:
  DB_PASSWORD: "REPLACE_ME"
  REDIS_PASSWORD: "REPLACE_ME"
  JWT_PRIVATE_KEY_PATH: "/app/keys/private.pem"
  JWT_PUBLIC_KEY_PATH: "/app/keys/public.pem"

  # Third-party integrations
  PLAID_CLIENT_ID: "REPLACE_ME"
  PLAID_SECRET: "REPLACE_ME"
  STRIPE_SECRET_KEY: "REPLACE_ME"
  STRIPE_WEBHOOK_SECRET: "REPLACE_ME"
  COMPLY_ADVANTAGE_API_KEY: "REPLACE_ME"
  JUMIO_API_TOKEN: "REPLACE_ME"
  JUMIO_API_SECRET: "REPLACE_ME"
  ONFIDO_API_TOKEN: "REPLACE_ME"
  ONFIDO_WEBHOOK_TOKEN: "REPLACE_ME"
  TWILIO_ACCOUNT_SID: "REPLACE_ME"
  TWILIO_AUTH_TOKEN: "REPLACE_ME"
  TWILIO_FROM_NUMBER: "REPLACE_ME"

  # Vault token for KMS (rotate frequently; prefer Vault agent)
  VAULT_TOKEN: "REPLACE_ME"
